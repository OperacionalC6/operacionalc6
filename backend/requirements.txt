fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1
pydantic[email]>=2.8
pydantic-settings>=2.4
python-jose[cryptography]>=3.3
google-auth>=2.34
requests>=2.32
python-multipart>=0.0.9
apscheduler>=3.10
playwright==1.46.0
httpx>=0.27
tenacity>=8.5
pyotp>=2.9
pandas>=2.2
openpyxl>=3.1
python-dateutil>=2.9
pillow>=10.4
# pywin32 é usado só pelo print dos dashboards da aba ACOMP_DIARIO
# (app/services/acomp_diario_evidencia.py), via automação COM do Excel —
# só funciona no Windows, onde este projeto sempre roda (ver skill
# project-context). Import é lazy (dentro da função), então o resto do
# projeto continua rodando normalmente sem essa dependência instalada.
pywin32>=306; sys_platform == "win32"
